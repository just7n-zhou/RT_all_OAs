# RT-CaseManager
Mock project for "What's your biggest Challenge" 

# Case Manager Mock (Local Full-Stack)
A simple local mock project:
- Backend: Node.js + Express + PostgreSQL (Docker)
- Frontend: React + TypeScript (Vite)
- Everything runs locally (no cloud)

---

## Prereqs

Make sure these are installed:

- Node.js 18+
- npm
- Docker Desktop (for PostgreSQL)

Check quickly:
```bash
node -v
npm -v
docker --version
